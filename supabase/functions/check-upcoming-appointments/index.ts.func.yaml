
name: check-upcoming-appointments
version: 1.0
description: Checks for upcoming appointments and medications to send notifications
schemas:
  - public
  - storage
  - auth
entrypoint: index.ts

# Run every 5 minutes
schedule:
  cron: '*/5 * * * *'
  run_on_startup: true
